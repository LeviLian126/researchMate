# ResearchMate API

Internal FastAPI deployment package for the ResearchMate engineering portfolio.

It is deployed together with the repository-level web application and AI worker. Product scope, API contracts, configuration boundaries, and release requirements are maintained in the repository root and the HTML documentation under `docs/`.
