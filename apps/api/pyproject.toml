[project]
name = "researchmate-api"
version = "0.1.0"
description = "FastAPI contract shell for ResearchMate"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.128.0",
  "pydantic>=2.12.0",
  "uvicorn>=0.40.0",
]

[tool.ruff]
line-length = 100

