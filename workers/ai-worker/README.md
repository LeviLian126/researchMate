# ResearchMate AI Worker

Internal Celery worker and transactional-outbox dispatcher package for the ResearchMate engineering portfolio.

It is deployed with the repository-level API and web application. Product scope, runtime contracts, configuration boundaries, and release requirements are maintained in the repository root and the HTML documentation under `docs/`.
