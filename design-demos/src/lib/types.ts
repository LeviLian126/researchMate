export type DemoView = "auth" | "chat" | "workspace";

export interface BrandConfig {
  name: string;
  mark: string;
}

export const brandConfig: BrandConfig = {
  name: "ResearchMate",
  mark: "R",
};
